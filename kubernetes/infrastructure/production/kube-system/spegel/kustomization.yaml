---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../../base/kube-system/spegel/ks.yaml

patches:
  - target:
      kind: Kustomization
      name: spegel
    patch: |-
      - op: replace
        path: /spec/path
        value: ./kubernetes/infrastructure/production/kube-system/spegel/app
