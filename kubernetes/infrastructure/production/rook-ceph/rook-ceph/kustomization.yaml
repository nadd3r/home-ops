---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../../base/rook-ceph/rook-ceph/ks.yaml

patches:
  - target:
      kind: Kustomization
      name: rook-ceph
    patch: |-
      - op: replace
        path: /spec/path
        value: ./kubernetes/infrastructure/production/rook-ceph/rook-ceph/app
  - target:
      kind: Kustomization
      name: rook-ceph-cluster
    patch: |-
      - op: replace
        path: /spec/path
        value: ./kubernetes/infrastructure/production/rook-ceph/rook-ceph/cluster
