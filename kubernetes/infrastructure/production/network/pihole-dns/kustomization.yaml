---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../../base/network/pihole-dns/ks.yaml

patches:
  - target:
      kind: Kustomization
      name: pihole-dns
    patch: |-
      - op: replace
        path: /spec/path
        value: ./kubernetes/infrastructure/production/network/pihole-dns/app
